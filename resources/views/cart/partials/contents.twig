<table class="table">
    <tr>
        <th>Product</th>
        <th>Quantity</th>
        <th>Price</th>
    </tr>
    {% for item in basket.all %}

    <tr>
        <td>{{ item.title }}</td>
        <td>{{ item.quantity }}</td>
        <td>€{{ item.price }}</td>
    </tr>
    
    {% endfor %}
</table>